package auth

import (
	"github.com/labstack/echo/v4"
)

// RegisterRoutes ลงทะเบียนเส้นทางสำหรับโมดูล auth
func AuthRoutes(e *echo.Echo) {
	// group := e.Group("/auth")
	// group.POST("/login",LoginHandler)    // ฟังก์ชัน LoginHandler
	// group.GET("/profile", ProfileHandler) // ฟังก์ชัน ProfileHandler
}
